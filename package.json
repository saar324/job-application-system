{
  "name": "job-application-system",
  "version": "0.2.0",
  "private": true,
  "description": "Agent-first, self-hosted, multi-profile job discovery and application orchestrator",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "worker": "node --env-file-if-exists=.env worker/server.js",
    "openclaw:bootstrap": "node scripts/bootstrap-openclaw.js",
    "state:migrate": "node scripts/migrate-state-to-sqlite.js",
    "eval:ranking": "node scripts/evaluate-ranking.js",
    "eval:adaptive": "node scripts/evaluate-adaptive.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "test": "node --test",
    "privacy:check": "node scripts/check-repository-privacy.js",
    "check:syntax": "find src worker scripts bin skills/job-application/scripts -name '*.js' -type f -print0 | xargs -0 -n1 node --check && bash -n scripts/deploy-systemd.sh docker-entrypoint.sh worker/docker-entrypoint.sh",
    "check": "npm run check:syntax && node --test && npm run privacy:check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@opentelemetry/api": "^1.9.1",
    "playwright": "^1.63.0",
    "zod": "^4.6.5"
  }
}
